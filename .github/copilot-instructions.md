# Copilot Instructions — pplx-spa-assets-knowledge

> Loaded automatically by GitHub Copilot coding agent.
> Canonical repository guidance lives in the root-level `.copilot-instructions.md`.

This file intentionally defers to `/.copilot-instructions.md` to avoid
conflicting or duplicated instructions.

## Source of truth

- Follow the root-level `.copilot-instructions.md` for all repository-wide guidance.
- If there is any overlap or conflict, the root-level `.copilot-instructions.md` takes precedence.
- Keep this `.github/copilot-instructions.md` file minimal and use it only as a pointer unless there is a GitHub-specific need that cannot live in the root file.
